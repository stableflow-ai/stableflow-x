/**
 * Legacy multi-route "continue" transfer is removed with the Rhea migration.
 * Historical continue actions are no longer supported in the UI.
 */
const ContinueTransfer = (_props: any) => {
  return null;
};

export default ContinueTransfer;

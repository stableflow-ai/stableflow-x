import Trusted from "@/views/bridge/components/trusted";

const TrustedBy = () => {
  return (
    <section className="mt-20 md:mt-30">
      <Trusted variant="about" />
    </section>
  );
};

export default TrustedBy;
